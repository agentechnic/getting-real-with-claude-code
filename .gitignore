# Workshop source / brainstorm — not part of the published site
Claude code workshop for beginners.md

# Claude Code project files — local context only (root-level only, not seed/)
CLAUDE.md
.claude/
!seed/.claude/
!seed/.claude/**

# The Nussaa seed ships its own CLAUDE.md — attendees need it in the clone.
!nussaa/CLAUDE.md

# macOS
.DS_Store
**/.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
.env
.venv/
venv/

# Attendees generate this during the workshop; it must not ship in the seed.
nussaa/themes-*.md
!nussaa/context/themes-2025-q4.md

# Internal working artifacts — planning docs, agent workspaces, tool caches.
# Attendees clone or download this repo; none of this is workshop content.
docs/
.superpowers/
.claude/
tools/.venv/
tools/uv.lock

# Separate video work, not part of the workshop site.
animations/

# Scratch notes and drafts. Workshop markdown lives in beats/, resources/,
# nussaa/, facilitator/ or archive/ — anything loose at the root is working
# material and should not be committed.
/*.md
!/README.md
!/DEPLOY.md
